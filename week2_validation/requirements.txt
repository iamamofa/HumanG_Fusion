# =============================================================================
# Week 2: Data Integrity & Statistical Validation — Python Dependencies
# =============================================================================
# This file lists all dependencies required to run every implemented feature.
# Dependencies are categorized by usage and requirement level.
# =============================================================================

# =============================================================================
# REQUIRED - Core runtime (pipeline will not run without these)
# =============================================================================

# Tabular data loading, schema validation, fusion/COSMIC DataFrames
# Used by: utils/data_loader, utils/state, adapters/week1_adapter, cosmic/diagnostics, run_week2, tests
pandas>=1.3.0

# Numerical arrays for all diagnostic modules
# Used by: distributions/visualize, distributions/log_normality, benford/diagnostics, benford/benford_controls (via diagnostics)
numpy>=1.20.0

# Histogram generation and descriptive visualization
# Used by: distributions/visualize (run_diagnostics, generate_histogram)
# Required when --run-diagnostics is used
matplotlib>=3.4.0

# Configuration and schema loading (thresholds.yaml, fusion_schema.yaml)
# Used by: config/loader, runtime/safe_runner, tests
PyYAML>=5.4.0

# =============================================================================
# OPTIONAL - Statistical enhancements (graceful degradation)
# =============================================================================
# The pipeline operates without these; some statistics are omitted with notes.

# Log-normality: Anderson-Darling statistic (without: AD not computed)
# Benford: chi-squared p-value (without: p-value not computed)
# Visualization: bias-corrected skewness (without: NumPy fallback used)
# COSMIC: Spearman correlation, bootstrap confidence intervals (1000 iterations), hypergeometric test, negative control
# Distribution: Bias-corrected skewness, Anderson-Darling statistic
# Benford: Chi-squared p-value
# Used by: distributions/log_normality, distributions/visualize, benford/diagnostics, cosmic/diagnostics, cosmic/statistical_controls
scipy>=1.7.0

# =============================================================================
# OPTIONAL - File format support
# =============================================================================
# Required only when loading files in the corresponding format.

# Excel (.xlsx, .xls) - data_loader uses engine="openpyxl" for read_excel
# Install only if fusion or COSMIC input is Excel format
openpyxl>=3.0.0

# Parquet (.parquet) - pandas read_parquet default engine
# Install only if fusion or COSMIC input is Parquet format
pyarrow>=6.0.0

# =============================================================================
# OPTIONAL - Runtime monitoring
# =============================================================================
# Used by: runtime/runtime_guard for memory limit checks.
# Without psutil: runtime guard skips memory checks; duration checks still apply.

psutil>=5.9.0

# =============================================================================
# DEVELOPMENT - For running integration tests
# =============================================================================
# Used by: tests/test_week2_integration.py
# Run tests: python -m pytest week2_validation/tests/
pytest>=7.0.0
